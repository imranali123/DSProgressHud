import XCTest
@testable import DSProgressHud

final class DSProgressHudTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(DSProgressHud(coder: <#NSCoder#>).text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
